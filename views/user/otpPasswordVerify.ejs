<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enter OTP</title>
    <style>
        body, html {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100%;
        }
        .container {
            background-image: url('reg-form/images/bg-registration-form-1.jpg');
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        
        .login_wrap {
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
            background: white;
        }
        .heading_s1 h3 {
            margin-bottom: 30px;
            font-size: 24px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group input[type="text"] {
            width: 89%;
            padding: 15px;
            border: 1px solid #ccc;
            border-radius: 10px;
            outline: none;
            font-size: 18px;
        }
        .form-group button {
            width: 100%;
            padding: 15px;
            background-color: #007bff;
            border: none;
            color: #fff;
            border-radius: 10px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s;
        }
        .form-group button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container">
 
    <div class="col-lg-4 offset-lg-4">
        <div class="login_wrap widget-taber-content">
            <div class="padding_eight_all">
                <div class="heading_s1">
                    <h3>Enter The OTP</h3>
                </div>
                <form method="post" action="/forgetpasswordverify">
                    <% if(locals.message) { %>
                    <div class="alert alert-danger"> <%= locals.message %></div>
                    <% } %>
                    <div class="form-group">
                        <input type="text" required="" name="otp" id="otp" placeholder="Enter OTP">
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-fill-out btn-block hover-up" name="login">Enter</button>
                    </div>
                </form>
                <form action="/forgetpassword/resend" method="post">
                    <button type="submit" id="resendButton" disabled>Resend</button>
                </form>
                <script>
                    var timeLeft = 30;
                    var resendButton = document.getElementById('resendButton');
                    var timerId = setInterval(countdown, 1000);
                    function countdown() {
                        if (timeLeft == 0) {
                            clearTimeout(timerId);
                            resendButton.innerHTML = 'Resend';
                            resendButton.disabled = false;
                        } else {
                            resendButton.innerHTML = 'Resend in ' + timeLeft + ' seconds';
                            timeLeft--;
                        }
                    }
                </script>
            </div>
        </div>
    </div>
</div>
</body>
</html>
